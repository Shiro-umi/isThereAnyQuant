package org.shiroumi.quant_kmp.ui.theme

import androidx.compose.material3.ColorScheme

actual fun syncLoadingChrome(colorScheme: ColorScheme) {
    // Android 不需要同步：首屏由 SplashScreen 或 windowBackground 直接渲染。
}
